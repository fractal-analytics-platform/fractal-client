# NOTE: This works if env variables are already defined,
#       and if the user is already registered

fractal project list
fractal task list
fractal dataset show $PROJECT_NAME $DATASET_IN_NAME
fractal dataset show-resources $PROJECT_NAME $DATASET_IN_NAME
fractal dataset show $PROJECT_NAME $DATASET_OUT_NAME
fractal dataset show-resources $PROJECT_NAME $DATASET_OUT_NAME
